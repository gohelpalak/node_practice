<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        transition: all .3s;
        transform-style: preserve-3d;
    }


    body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: url('https://img.freepik.com/premium-photo/path-through-forest-with-light-ground-trees-are-covered-moss_421632-845.jpg?w=2000');
        background-size: cover;
        background-position: center;
    }

    .input-box input {
        width: 100%;
        margin: 10px 0;
        height: 100%;
        background: transparent;
        font-size: 16px;
        color: #fff;
        padding: 20px 40px 20px 20px;
        border: none;
        outline: none;
        border: 2px solid rgba(255, 255, 255, .2);
        border-radius: 40px;
    }


    .screen {
        width: 320px;
        height: 550px;
        background: #fff;
        border-radius: 40px;
        position: relative;
        transform: rotateY(30deg) rotateX(-30deg);
        background: rgba(255, 255, 255, 0.09);
        border-radius: 16px;
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.3);
        animation: animate 20s linear infinite;
        /* margin: 30px 0; */
    }
    .btn{
    width: 100%;
    height: 45px;
    background: #fff;
    border: none;
    outline: none;
    border-radius: 40px;
    box-shadow: 0 0 10px rgba(0, 0, 0, .1);
    cursor: pointer;
    color: #333;
    font-weight: 600;
    transition: .5s;
}


    @keyframes animate {
        0% {
            transform: rotateY(30deg) rotateX(-30deg);
        }

        25% {
            transform: rotateY(20deg) rotateX(30deg);
        }

        50% {
            transform: rotateY(-20deg) rotateX(20deg);
        }

        75% {
            transform: rotateY(-20deg) rotateX(-30deg);
        }

        100% {
            transform: rotateY(30deg) rotateX(-30deg);
        }
    }


    .screen:before {
        content: "";
        position: absolute;
        top: 0;
        left: 50%;
        transform: translate(-50%, 0);
        width: 120px;
        height: 25px;
        border-bottom-right-radius: 20px;
        border-bottom-left-radius: 20px;
        background: rgba(255, 255, 255, 0.09);
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);



    }
</style>

<body>
    <div class="screen">
        <form action="/movie/addmovie" method="post">
        <div class="input-box">
            <input type="text" placeholder="Movie title" style="color: black;" name="title">
        </div>

        <div class="input-box">
            <input type="text" placeholder="Description" name="desc">
        </div>

        <div class="input-box">
            <input type="text" placeholder="Genre" name="Genre">
        </div>

        <div class="input-box">
            <input type="text" placeholder="Language" name="Lang">
        </div>

        <div class="input-box">
            <input type="number" placeholder="Time" name="Time">
        </div>

        <div class="input-box">
            <input type="file" placeholder="file" name="image">
        </div>

       

        <button type="submit" class="btn">Add Movie</button>
    </form>

    </div>
</body>

</html>