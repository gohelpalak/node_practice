<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book - Book Store</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <!-- Header -->
  <header class="w-full bg-teal-600 text-white py-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center px-6">
      <h1 class="text-2xl font-bold">Book Store</h1>
      <a href="/" class="text-lg hover:text-teal-300 transition">Home</a>
    </div>
  </header>

  <!-- Edit Book Form -->
  <main class="py-10 w-full max-w-3xl mx-auto">
    <div class="bg-white shadow-xl rounded-lg p-8">
      <h2 class="text-2xl font-bold text-gray-800 mb-6">Edit Book Details</h2>
      <form id="book-form" class="space-y-6" method="post" action="/books/editBook/<%=singleBook.id %>">
        <!-- Form Fields -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          <!-- Book Name -->
          <div>
            <label for="book-name" class="block text-gray-700 font-medium">Book Name</label>
            <input type="text" value="<%= singleBook.bookName %>" name="bookName" placeholder="Enter book name"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>

          <!-- Book Title -->
          <div>
            <label for="book-title" class="block text-gray-700 font-medium">Book Title</label>
            <input type="text" value="<%= singleBook.bookTitle %>" name="bookTitle" placeholder="Enter book title"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>

          <!-- Author Name -->
          <div>
            <label for="author-name" class="block text-gray-700 font-medium">Author Name</label>
            <input type="text" value="<%= singleBook.authorName %>" name="authorName" placeholder="Enter author name"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>

          <!-- Publish Date -->
          <div>
            <label for="publish-date" class="block text-gray-700 font-medium">Publish Date</label>
            <input type="date" value="<%= singleBook.publishDate %>" name="publishDate"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>

          <!-- Book Price -->
          <div>
            <label for="book-price" class="block text-gray-700 font-medium">Price (in INR)</label>
            <input type="number" value="<%= singleBook.bookPrice %>" name="bookPrice" placeholder="Enter price"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>

          <!-- Book Pages -->
          <div>
            <label for="book-pages" class="block text-gray-700 font-medium">Pages</label>
            <input type="number" value="<%= singleBook.bookPages %>" name="bookPages" placeholder="Enter pages"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>

          <!-- Book Image URL -->
          <div class="sm:col-span-2">
            <label for="book-image" class="block text-gray-700 font-medium">Book Image URL</label>
            <input type="url" value="<%= singleBook.bookImage %>" name="bookImage" placeholder="Enter image URL"
              class="mt-2 w-full border border-gray-300 rounded-lg p-3 focus:outline-none focus:ring-2 focus:ring-teal-500">
          </div>
        </div>

        <!-- Submit Button -->
        <div class="text-right">
          <button type="submit"
            class="bg-teal-600 text-white py-3 px-6 rounded-lg font-semibold hover:bg-teal-700 transition duration-300">
            Save Changes
          </button>
        </div>
      </form>
    </div>
  </main>
</body>
</html>
